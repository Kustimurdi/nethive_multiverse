```{julia}
folder_path_1 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c1000e_lr5"
#Das ist die Basis, der erste grosse run. Problem, manche runs waren nicht lang genug, die muessen gefiltert werden
#folder_path_2 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c1000e_lr5_rest"
#Das sind die runs, die bei dem ersten gescheitert sind, weil die Zeit zu kurz war. Ich vermute, dass die alle nicht brauchbar sind, weil ich wieder 1000 epochs genutzt habe
folder_path_3 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e50+500dt"
folder_path_4 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e100dt"
#Die zwei folder haben die runs von folder 1, die in meinen augen es benoetigt haben, mit 10000 epochs gerunt zu werden, sprich, diese sollten bei folder 1 rausgefiltert werden und von hier uebernommen werden

#bis hierhin: 
# dt:  0.01, 0.05, ..., 500, 1000
# int: 0.01, 0.05, ..., 500, 1000

folder_path_5 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c1000e_dt_low_between"
#ab hier werden Werte von dead time aufgefuellt, die davor nicht dabei waren. als erstes die niedrigen Werte, die nicht lange runnen muessen, um den stable state zu erreichen. Das muss ich mir nochmal anschauen, weil es trotzdem niedrige interaction rates gibt, sprich es dauert lange, bis was passiert. Es koennte sein, dass der Bereich mit genug Zeit funktioniert, wenn die dead time lang genug ist. 
folder_path_6 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e_dt_mid_between"
folder_path_7 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e_between_vals_high_dt"
#Die zwei run sind die restlichen Bereich von der dead time intermed values. Beides hat hohe Werte an dead time, also muessen sie evtl laenger simuliert werden

#bis hierhin:
# dt:  0.01, 0.02, 0.03, ..., 800, 900, 1000
# int: immer noch gleich

folder_path_8 = "/project/theorie/n/N.Pfaffenzeller/results_project/10b10t/10b10t15c1000e_10-dt80-int"

#folder_path_8 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c1000e_interact_low_intermed_vals"
#das ist der erste run, um die interaction rate values aufzufuellen. hier benutze ich 1000 epochs und ich bin mir nicht sicher, ob das passt. Das haengt davon ab, welche dead time values ich benutzt habe. Ich muss das inspizieren. 
#folder_path_7 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e_high_interact_all_dt"
#folder_path_8 = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs/10b10t15c10000e_low_interact_high_dt"
#die beiden laufen noch

folder_path_9 = "/project/theorie/n/N.Pfaffenzeller/results_project/10b10t/10b10t15c1000e10-dt70+int"

```

```{julia}
summaries_1 = CSV.read(joinpath(folder_path_1, "summaries.csv"), DataFrame)
#summaries_2 = CSV.read(joinpath(folder_path_2, "summaries.csv"), DataFrame)
summaries_3 = CSV.read(joinpath(folder_path_3, "summaries.csv"), DataFrame)
summaries_4 = CSV.read(joinpath(folder_path_4, "summaries.csv"), DataFrame)
summaries_5 = CSV.read(joinpath(folder_path_5, "summaries.csv"), DataFrame)
summaries_6 = CSV.read(joinpath(folder_path_6, "summaries.csv"), DataFrame)
summaries_7 = CSV.read(joinpath(folder_path_7, "summaries.csv"), DataFrame)
summaries_8 = CSV.read(joinpath(folder_path_8, "summaries.csv"), DataFrame)
summaries_9 = CSV.read(joinpath(folder_path_9, "summaries.csv"), DataFrame)
#summaries_10 = CSV.read(joinpath(folder_path_10 "summaries.csv"), DataFrame)
```

```{julia}
println("--------------------------------------------")
println("summaries 1 info:")
println("foldername: ", basename(folder_path_1))
println("interaction rates: ", sort(unique(summaries_1.interaction_rate)))
println("dead times: ", sort(unique(summaries_1.dead_time)))
println("#epochs used: ", unique(summaries_1.n_epochs))
println("learning rate: ", unique(summaries_1.learning_rate))
println("--------------------------------------------")
```

```{julia}
println("--------------------------------------------")
println("foldername: ", basename(folder_path_2))
println("summaries 2 info:")
println("interaction rates: ", sort(unique(summaries_2.interaction_rate)))
println("dead times: ", sort(unique(summaries_2.dead_time)))
println("this one should be useless because, epoch used: ", unique(summaries_2.n_epochs))
println("--------------------------------------------")
```

```{julia}
println("--------------------------------------------")
println("summaries 3 info:")
println("foldername: ", basename(folder_path_3))
println("interaction rates: ", sort(unique(summaries_3.interaction_rate)))
println("dead times: ", sort(unique(summaries_3.dead_time)))
println("#epochs used: ", unique(summaries_3.n_epochs))
println("learning rate: ", unique(summaries_3.learning_rate))
println("--------------------------------------------")
```

```{julia}
println("--------------------------------------------")
println("summaries 4 info:")
println("foldername: ", basename(folder_path_4))
println("interaction rates: ", sort(unique(summaries_4.interaction_rate)))
println("dead times: ", sort(unique(summaries_4.dead_time)))
println("#epochs used: ", unique(summaries_4.n_epochs))
println("learning rate: ", unique(summaries_4.learning_rate))
println("--------------------------------------------")
```

```{julia}
println("--------------------------------------------")
println("summaries 5 info:")
println("foldername: ", basename(folder_path_5))
println("interaction rates: ", sort(unique(summaries_5.interaction_rate)))
println("dead times: ", sort(unique(summaries_5.dead_time)))
println("#epochs used: ", unique(summaries_5.n_epochs))
println("learning rate: ", unique(summaries_5.learning_rate))
println("--------------------------------------------")
```

# removing the git commit, git dirty, julia version columns as they dont exist for the first run

```{julia}
select!(summaries_3, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_4, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_5, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_6, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_7, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_8, Not([:git_commit, :git_dirty, :julia_version]))
select!(summaries_9, Not([:git_commit, :git_dirty, :julia_version]))
#select!(summaries_8, Not([:git_commit, :git_dirty, :julia_version]))
```

# filtering out the runs from the og run with 1000 epochs and high dt
# filtering out the failed runs in summaries 8

```{julia}
dropmissing!(summaries_8)
summaries_8.interaction_rate = parse.(Float64, summaries_8.interaction_rate)
sum_8_filt = filter(row -> row.interaction_rate < 80, summaries_8)
```

```{julia}
sums_1_filtered = filter(row -> row.dead_time < 50 && row.interaction_rate < 1000, summaries_1)
```

# concatenating the summaries.

```{julia}
ult_sum = vcat(sums_1_filtered, summaries_3, summaries_4, summaries_5, summaries_6, summaries_7, sum_8_filt)
sum_wout_intemed = vcat(sums_1_filtered, summaries_3, summaries_4)
sum_dt = vcat(sums_1_filtered, summaries_3, summaries_4, summaries_5, summaries_6, summaries_7)
```




```{julia}
#f_sum = copy(sum_wout_intemed)
#f_sum = copy(ult_sum)
#f_sum = copy(all_og_vals_sum)
#f_sum = copy(summaries_1)
#f_sum = copy(summaries_8)
#f_sum = copy(sum_8_filt)
f_sum = copy(sum_dt)
```

# filtering the data

```{julia}
filter_1 = :lambda_sensitivity
filter_1_val = 100
filter_2 = :learning_rate
filter_2_val = 1.0e-5
filter_2_val = 5.0e-5
f_sum = filter(row -> row[filter_1] == filter_1_val, f_sum)
f_sum = filter(row -> row[filter_2] == filter_2_val, f_sum)
#f_sum = filter(row -> row.lambda_sensitivity == 100, f_sum)
#f_sum = filter(row -> row.learning_rate == 5.0e-5, f_sum)
#f_sum = filter(row -> row.dead_time == 5.0, f_sum)
```

# aggregating the data

```{julia}
xcol = "dead_time"
ycol = "interaction_rate"
#xcol = "n_bees"
#ycol = "n_tasks"
metric_col = :coverage_fraction
groupcols = [Symbol(xcol), Symbol(ycol)]
agg = combine(groupby(f_sum, groupcols),
              metric_col => mean => :mean_coverage,
              metric_col => std  => :std_coverage,
              metric_col => length => :coverage_count,
              :p_end_mean => mean => :mean_score)

sort!(agg, groupcols)
```

# prepare data for the heatmap

```{julia}
xcol = Symbol(xcol)
ycol = Symbol(ycol)
valcol = :mean_coverage
#valcol = :mean_score

xs = sort(unique(agg[:, xcol]))
ys = sort(unique(agg[:, ycol]))
#ys = sort(parse.(Float64, unique(agg[:, ycol])))
xi = Dict(v => i for (i,v) in enumerate(xs))
yi = Dict(v => i for (i,v) in enumerate(ys))

mat = fill(NaN, length(xs), length(ys))
for r in eachrow(agg)
    mat[xi[r[xcol]], yi[r[ycol]]] = r[valcol]
    #mat[xi[r[xcol]], yi[parse(Float64, r[ycol])]] = r[valcol]
end
```

```{julia}
heat_fig = Figure(resolution=(800,600))
ax = Axis(heat_fig[1,1], xlabel = string(xcol), ylabel = string(ycol), title="run $(basename(folder_path)): $(string(valcol))")

# draw on integer grid and keep the mapping via tick labels
# plot transposed matrix on x=1:length(xs), y=1:length(ys) so xticks->xs and yticks->ys
hm = heatmap!(ax, 1:length(xs), 1:length(ys), mat; colormap = :viridis)
ax.xticks = (1:length(xs), string.(xs))
ax.yticks = (1:length(ys), string.(ys))

Colorbar(heat_fig[1,2], hm)
```

```{julia}
display(heat_fig)
```

```{julia}
folder_path = "/scratch/n/N.Pfaffenzeller/nikolas_nethive/nethive_multiverse/results/big_runs"
save_path = joinpath(folder_path, "plots")
if !isdir(save_path)
    mkdir(save_path)
end
name = "10b10t_log_plot_$(string(valcol))_og_log_plot"
#name = "$(basename(folder_path))_$(string(valcol))_$(filter_1)_$(filter_1_val)_$(filter_2)_$(filter_2_val)"
save(joinpath(save_path, "$(name).pdf"), heat_fig)
save(joinpath(save_path, "$(name).png"), heat_fig)
```